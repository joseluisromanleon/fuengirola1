<?php

namespace Database\Seeders;

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class ModifyTeams extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
//        $team = Team::create(['name' => 'Aplicants']); // Crea el equipo

       /*
       $users = User::all(); // Asume que ya tienes usuarios registrados

        foreach ($users as $user) {
            TeamUser::create([
                'user_id' => $user->id,
                'team_id' => $team->id,
                'role' => 'propietario' // Asigna el rol de propietario
            ]);
        }
       */
    }
}
